import { neon } from "@neondatabase/serverless";
// Quick check: show all user_role_assignments after demo cleanup
console.log("Role assignment count check - write a small query to see all active assignments");
